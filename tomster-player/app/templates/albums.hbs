<div class="row">
  <div class="col-4">
    <div class="list-group">
      {{#each this.model as |album|}}
        <LinkTo @route="albums.album" @model={{album.id}} class="list-group-item list-group-item-action">
          {{album.title}}
        </LinkTo>
      {{/each}}
    </div>
  </div>

  {{!-- Selected album --}}
  <div class="col-8">
    {{outlet}}
  </div>
</div>
